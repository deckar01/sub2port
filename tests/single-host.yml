services:
  sub2port:
    image: sub2port
    ports:
      - "18081:80"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
  app:
    image: traefik/whoami
    environment:
      SUB2PORT: app.test:80
