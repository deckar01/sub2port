services:
  sub2port:
    image: sub2port
    ports:
      - "18086:80"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
  app1:
    image: traefik/whoami
    environment:
      SUB2PORT: app.test:80
  app2:
    image: traefik/whoami
    environment:
      SUB2PORT: app.test:80
